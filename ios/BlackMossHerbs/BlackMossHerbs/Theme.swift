import SwiftUI

struct Theme {
    static let primary = Color(red: 0.22, green: 0.33, blue: 0.22) // Deep Earth Green
    static let secondary = Color(red: 0.85, green: 0.73, blue: 0.40) // Herbal Gold
    static let background = Color.black // Gucci/Apple premium dark mode feel
    static let surface = Color(red: 0.1, green: 0.1, blue: 0.1)
    static let text = Color.white
    static let textMuted = Color.gray
}
