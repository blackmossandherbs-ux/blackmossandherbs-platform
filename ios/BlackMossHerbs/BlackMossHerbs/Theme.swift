import SwiftUI

struct Theme {
    static let primary = Color(red: 0.561, green: 0.447, blue: 0.137) // Antique Gold
    static let secondary = Color(red: 0.831, green: 0.686, blue: 0.216) // Classic Gold
    static let background = Color.black // Gucci/Apple premium dark mode feel
    static let surface = Color(red: 0.063, green: 0.055, blue: 0.047) // Jet Black
    static let text = Color.white
    static let textMuted = Color.gray
}
