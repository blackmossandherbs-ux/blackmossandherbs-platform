# Catch-all server to prevent "confusion" with other sites
# This block will handle any request that doesn't match a specific server_name
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444; # Drop the connection
}
